language: objective-c
osx_image: xcode7.2
xcode_workspace: ESFramework.xcworkspace
xcode_scheme: ESFramework
before_install:

    - env
    - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
    - pod install --no-repo-update

script:

    - pod lib lint --allow-warnings

